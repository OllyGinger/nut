[package]
name = "bootloader-x86_64-bios-stage0"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[profile.dev]
panic = "abort"
lto = false

[profile.release]
panic = "abort"
lto = false