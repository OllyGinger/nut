[package]
name = "nut"
version = "0.1.0"
edition = "2021"

[dependencies]

[workspace]
members = [
    "bootloader/bios/stage0"
]

[profile.dev]
panic = "abort"
lto = false

[profile.release]
panic = "abort"
lto = false

[build-dependencies]
async-process = "1.6.0"
futures = "0.3.25"
futures-concurrency = "7.0.0"
